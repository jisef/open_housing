export interface Room {
  bedrooms: number;
  capacity: number;
  has_kitchen: boolean;
  is_apartment: boolean;
  max_capacity: number;
  name: string;
  number: number;
  room_pk: number;
  valid: boolean;
}